describe('checkPerms', () => {
  it('should return True if command is public and does not require any perms.', () => {
    let settings = {
      enabled: true,
      public: true,
      PM: false,
      owneronly: false,
      permissionsRequired: []
    }
  })
})
