import { expect } from 'mocha'

describe('getCommandSettings', () => {
    it('Should return an object', () => {

    })
})
describe('addDefaultdata', () => {

})
describe('checkCommandPerms', () => {
    it('Should return true if the command is enabled, member has perms', () => {

    })
    it('Should return false if the command is not enabled in PMs and the channel type is PM', () => {

    })
    it('Should return false if command is not enabled or member has no perms', () => {

    })
})