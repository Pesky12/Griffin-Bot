{
    "version": "0.2.0",
    "configurations": [
        {
            "preLaunchTask": "build",
            "name": "Build and run",
            "type": "node",
            "request": "launch",
            "envFile": "${workspaceFolder}/.env",
            "args": ["${workspaceRoot}/dist/app"],
            "cwd": "${workspaceRoot}/dist",
            "protocol": "inspector",
            "internalConsoleOptions": "openOnSessionStart"
        }
    ],
}
